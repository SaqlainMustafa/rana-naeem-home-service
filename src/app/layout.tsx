import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "RTS Services - Professional Technical Services",
  description: "Professional technical services including electrical, plumbing, tiles, glass fixing, aluminium works, painting, and more. Quality workmanship with reliable service.",
  keywords: "technical services, electrical, plumbing, tiles, glass fixing, aluminium works, painting, maintenance, Dubai, UAE",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
